using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class SpawnerFruits : MonoBehaviour
{
    //spawna frutta ogni tempoSpawn secondi in un punto casuale tra i punti di spawn
    // Start is called before the first frame update

    /*
    using System.Collections.Generic;
    using UnityEngine;

    public class Example : MonoBehaviour
    {
        // Dichiarazione della lista di Transform
        private List<Transform> transformsList;

        void Start()
        {
            // Inizializzazione della lista
            transformsList = new List<Transform>();

            // Aggiunta di Transform alla lista (ad esempio, aggiungendo i figli di questo GameObject)
            foreach (Transform child in transform)
            {
                transformsList.Add(child);
            }

            // Stampa dei nomi dei Transform nella lista
            foreach (Transform t in transformsList)
            {
                Debug.Log(t.name);
            }
        }
    }
    Crea figli oggetto su cui sarà questo script in posizione degli spawnpoint
    */
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
